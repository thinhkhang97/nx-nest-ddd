{
  "$schema": "http://json-schema.org/schema",
  "$id": "DomainEvent",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    },
    "domain": {
      "type": "string",
      "description": "Domain contains this event",
      "$default": {
        "$source": "argv",
        "index": 1
      },
      "x-prompt": "What domain would you like to use?"
    },
    "sourceRoot": {
      "type": "string",
      "description": "The directory where the new event will be created. This option will override 'domain' argument"
    },
    "templatePath": {
      "type": "string",
      "description": "A directory where you place the custom template"
    }
  },
  "required": ["name", "domain"]
}
